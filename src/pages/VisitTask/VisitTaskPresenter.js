import React from 'react';
import PropTypes from 'prop-types';
import Calendar from '../../components/Date/WPCalendar';
const VisitTaskPresenter = props => {
  return <Calendar />;
};

VisitTaskPresenter.propTypes = {};

export default VisitTaskPresenter;
